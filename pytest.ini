[pytest]
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning:websockets.*
    # gr.Warning() surfaces as a UserWarning when called outside a live request.
    ignore:.*:UserWarning:gradio.helpers
